<Window x:Class="AirborneDustMonitor.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AirborneDustMonitor.UI.Views.Charts"
        Title="Sensor Dashboard" Height="800" Width="1000">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <local:PMChartView DataContext="{Binding PMVM}" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <local:TemperatureChartView DataContext="{Binding TemperatureVM}" Grid.Row="0" Grid.Column="1" Margin="5"/>
        <local:HumidityChartView DataContext="{Binding HumidityVM}" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <local:BatteryChartView DataContext="{Binding BatteryVM}" Grid.Row="1" Grid.Column="1" Margin="5"/>
    </Grid>
</Window>
