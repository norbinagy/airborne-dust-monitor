<UserControl x:Class="AirborneDustMonitor.UI.Views.MetricDisplayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AirborneDustMonitor.UI.Views"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="100" />
        </Grid.ColumnDefinitions>
        <oxy:PlotView Model="{Binding PlotModel}" Grid.Column="0"/>
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock Text="Min:" FontWeight="Bold"/>
            <TextBlock Text="{Binding MinValue}"/>
            <TextBlock Text="Max:" FontWeight="Bold"/>
            <TextBlock Text="{Binding MaxValue}"/>
            <TextBlock Text="Mozgóátlag:" FontWeight="Bold"/>
            <TextBlock Text="{Binding MovingAverage}"/>
            <TextBlock Text="Riasztási szint:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ThresholdValue, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:F1}}" Width="80" HorizontalAlignment="Left" Padding="3"/>
            <TextBlock Text="Jelenlegi:" Margin="0,10,0,0"/>
            <TextBlock Text="{Binding ThresholdValue, StringFormat={}{0:F2}}"/>
            <TextBlock Text="Aktív riasztások:" FontWeight="Bold" Foreground="Red" Margin="0,15,0,5"/>
            <ItemsControl ItemsSource="{Binding AlertingSensors}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FFE5E5" BorderBrush="Red" BorderThickness="1" 
                        CornerRadius="4" Margin="0,2" Padding="5,2">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚠️ Szenzor " Foreground="DarkRed"/>
                                <TextBlock Text="{Binding}" Foreground="DarkRed"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
